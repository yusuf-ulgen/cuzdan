<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-black"
        android:text="Portföyü Düzenle"
        android:textColor="?attr/textPrimary"
        android:textSize="20sp" />

    <EditText
        android:id="@+id/edit_portfolio_name"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="20dp"
        android:background="@drawable/bg_input_field"
        android:hint="Portföy Adı"
        android:paddingHorizontal="16dp"
        android:textColor="?attr/textPrimary"
        android:textColorHint="?attr/textSecondary"
        android:textSize="16sp" />

    <com.google.android.material.switchmaterial.SwitchMaterial
        android:id="@+id/switch_include_total"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Portföyler Toplamına Dahil Et"
        android:textColor="?attr/textPrimary"
        android:fontFamily="sans-serif-medium"
        app:thumbTint="?attr/accentColor"
        app:trackTint="?attr/divider_light" />

    <Button
        android:id="@+id/btn_save"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_marginTop="32dp"
        android:backgroundTint="?attr/accentColor"
        android:fontFamily="sans-serif-black"
        android:text="GÜNCELLE"
        android:textColor="@color/white"
        app:cornerRadius="30dp" />

    <Button
        android:id="@+id/btn_delete"
        style="@style/Widget.MaterialComponents.Button.TextButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Bu Portföyü Sil"
        android:textColor="@color/accent_red"
        android:fontFamily="sans-serif-medium"
        app:rippleColor="#33FF5252" />

</LinearLayout>
